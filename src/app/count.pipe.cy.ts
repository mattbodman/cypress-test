import { CountPipe } from './count.pipe';

// const pipe = new CountPipe();
describe('CountPipe', () => {
  /*  it('returns string with count', () => {
    expect(pipe.transform('food')).to.equal('food [4]');
  });
  it('returns empty string with empty string input', () => {
    expect(pipe.transform('')).to.equal('');
  });*/
});
